# Sonora Web — environment template. Copy to `.env` for local dev.
# In production (Vercel) set this as an environment variable instead.

# Base URL of the Sonora API.
# Local dev: http://localhost:3000
# Production: https://<your-api>.onrender.com
VITE_API_URL=http://localhost:3000
